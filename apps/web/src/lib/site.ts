/** Public site origin for canonical URLs, sitemap and Open Graph. */
export const siteUrl = process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3001";

/** Optional public support address; contact details are hidden when unset. */
export const supportEmail = process.env.NEXT_PUBLIC_SUPPORT_EMAIL ?? "";
